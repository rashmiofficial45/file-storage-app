/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                hostname: 'notable-kiwi-908.convex.cloud',
            }
        ]
    }
};
export default nextConfig;
